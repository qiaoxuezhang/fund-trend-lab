# 隐私说明

更新日期：2026年8月10日

## 本地保存的数据

以下信息只保存在使用者当前浏览器的 `localStorage` 中：

- 自选基金和客户分组
- 持仓参考金额、推算份额和客户填写的成本净值
- 默认及单基金止盈目标
- 风险风格、主题和通知开关

服务端没有用户数据库，也没有用于保存这些信息的接口。新浏览器首次打开时默认组合为空。

## 服务端临时处理的数据

为了读取公开基金净值、持仓和公司资料，浏览器会向服务端发送需要查询的基金代码。组合代码列表使用 HTTPS POST 请求体发送，避免出现在普通 URL 查询串中。

服务端只缓存公开市场数据，不缓存客户分组、持仓金额、持仓成本、收益目标或保险箱密码。云平台本身仍可能保留网络元数据和基础访问日志，因此生产部署必须使用可信服务商和 HTTPS。

## 加密保险箱

跨设备迁移使用浏览器 Web Crypto API：

- 密钥派生：PBKDF2-SHA-256，250,000 次迭代
- 数据加密：AES-256-GCM
- 密码不保存、不上传，也无法由服务端找回

保险箱文件和密码应分开保存。忘记密码后无法恢复加密内容。

## 第三方公开数据

基金净值、季度持仓、上市公司财报、公告和新闻来自公开数据接口。访问这些接口时由本项目服务端发起请求，不会附带客户的持仓金额、成本、分组或收益目标。

## 多人使用边界

本版本采用“每个浏览器一份本地保险箱”的隔离方式，不提供服务器账户同步。共享同一浏览器系统账户的人可能访问该浏览器保存的数据，因此公共电脑应使用独立浏览器用户配置或无痕窗口，并在离开前清理本地数据。

网页的“隐私与跨设备”弹窗提供二次确认的本机数据清除按钮。清除前应先导出加密保险箱；清除后服务器无法恢复这些本地数据。
