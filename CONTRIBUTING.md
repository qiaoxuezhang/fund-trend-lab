# 参与开发

## 本地运行

项目要求 Node.js 20 或更高版本，不需要安装第三方依赖：

```bash
npm start
```

运行测试：

```bash
npm test
```

## 提交要求

- 不提交 `.cache`、`.env`、保险箱文件和真实客户数据。
- 修改指标时必须补充测试，并说明阈值和金融含义。
- 页面文案不得承诺收益或将历史指标描述为确定性预测。
- 新增数据源时应设置超时、输入校验和失败降级。
- 前端改动需要检查电脑端与手机端横向溢出和控制台错误。
